def suma(unNumero: float, otroNumero: float) -> float:
    """
    POST: Devuelve la suma de los numeros ingresados.
    TESTS:
    >>> suma(2, 5)
    7
    >>> suma(-3, 5)
    2
    >>> suma(2.4, 0)
    2.4
    """
    return unNumero + otroNumero
